name: hermes-hmem
version: "1.0.0"
description: "its-over-9k (hmem) integration for Hermes — per-turn exchange logging plus session-end checkpoint. Requires the `hmem` CLI on PATH (npm i -g its-over-9k)."
author: bumblebiber
hooks:
  - on_session_start
  - pre_llm_call
  - post_llm_call
